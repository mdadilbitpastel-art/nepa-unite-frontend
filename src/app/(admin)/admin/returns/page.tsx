"use client";

import { ReturnsManagement } from "@/components/shared/returns-management";

export default function AdminReturnsPage() {
  return (
    <ReturnsManagement
      title="Returns oversight"
      description="Monitor and override return/exchange requests across all sellers."
    />
  );
}
