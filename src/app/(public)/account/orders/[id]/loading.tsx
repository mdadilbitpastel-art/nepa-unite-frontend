import { DetailSkeleton } from "@/components/shared/states";

/** Instant skeleton shown while an order detail route loads. */
export default function OrderDetailLoading() {
  return <DetailSkeleton />;
}
